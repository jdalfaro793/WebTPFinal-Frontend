import { EstadonoticiaPipe } from './estadonoticia.pipe';

describe('EstadonoticiaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadonoticiaPipe();
    expect(pipe).toBeTruthy();
  });
});
